<h1>{{title}}</h1>
{{!!-- comments can be display with either this notation --}}
{{! or this notation }}
{{!!-- using this style allows you to add in braces --}}
<h2>{{article.title}}</h2>
{{#list people}}{{firstName}} {{lastName}}{{/list}}
<div class="entry">
{{!!-- example of passing a context into a helper --}}
  {{#with article}}
  <div class="intro">{{{intro}}}</div>
  <div class="body">{{{body}}}</div>
  {{/with}}
</div>